import { useState } from 'react'
import { useApp } from '../context/AppContext'
import { Button } from './ui/Button'
import { Modal } from './ui/Modal'

export default function SettingsModal({open,onClose}:{open:boolean;onClose:()=>void}){const {theme,toggleTheme,showToast}=useApp();const [language,setLanguage]=useState('English');const [fontSize,setFontSize]=useState('Medium');const [model,setModel]=useState('GPT-4o-mini');const [notifications,setNotifications]=useState(true);const save=()=>{showToast('Settings saved');onClose()};return <Modal open={open} onClose={onClose} title="Settings"><div className="space-y-4"><Setting label="Theme"><button onClick={toggleTheme} className="rounded-lg border border-slate-200 px-3 py-2 text-xs font-semibold">{theme==='light'?'Light':'Dark'} mode</button></Setting><Setting label="Language"><select value={language} onChange={e=>setLanguage(e.target.value)} className="field"><option>English</option><option>Hindi</option><option>French</option></select></Setting><Setting label="Font Size"><select value={fontSize} onChange={e=>setFontSize(e.target.value)} className="field"><option>Small</option><option>Medium</option><option>Large</option></select></Setting><Setting label="AI Model"><select value={model} onChange={e=>setModel(e.target.value)} className="field"><option>GPT-4o-mini</option><option>GPT-4.1-mini</option><option>Enterprise RAG</option></select></Setting><Setting label="Notifications"><button role="switch" aria-checked={notifications} onClick={()=>setNotifications(!notifications)} className={`relative h-6 w-11 rounded-full transition ${notifications?'bg-blue-600':'bg-slate-300'}`}><span className={`absolute top-1 h-4 w-4 rounded-full bg-white transition ${notifications?'left-6':'left-1'}`}/></button></Setting><Setting label="Privacy"><span className="text-[10px] text-slate-500">Conversation data stays in this browser.</span></Setting></div><div className="mt-6 flex justify-end gap-2"><Button variant="secondary" onClick={onClose}>Cancel</Button><Button onClick={save}>Save settings</Button></div></Modal>}
function Setting({label,children}:{label:string;children:React.ReactNode}){return <div className="flex items-center justify-between gap-4 rounded-xl border border-slate-200 p-3"><div><p className="text-xs font-semibold">{label}</p><p className="text-[9px] text-slate-400">Configure {label.toLowerCase()}</p></div>{children}</div>}
