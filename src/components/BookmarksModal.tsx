import { Bookmark, Trash2 } from 'lucide-react'
import { useApp } from '../context/AppContext'
import { Modal } from './ui/Modal'

export default function BookmarksModal({open,onClose}:{open:boolean;onClose:()=>void}){const {bookmarks,updateMessage}=useApp();return <Modal open={open} onClose={onClose} title="Bookmarked answers"><div className="max-h-[55vh] space-y-3 overflow-y-auto">{bookmarks.map(message=><div key={message.id} className="rounded-xl border border-slate-200 p-4"><div className="flex items-start gap-3"><Bookmark size={16} className="mt-0.5 shrink-0 text-blue-600" fill="currentColor"/><div className="min-w-0 flex-1"><p className="text-xs font-semibold leading-5">{message.content}</p>{message.detail&&<p className="mt-1 line-clamp-2 text-[10px] leading-4 text-slate-500">{message.detail}</p>}</div><button onClick={()=>updateMessage(message.id,{bookmarked:false})} className="text-slate-400 hover:text-red-600" aria-label="Remove bookmark"><Trash2 size={14}/></button></div></div>)}{!bookmarks.length&&<div className="rounded-xl border border-dashed border-slate-200 p-8 text-center"><Bookmark className="mx-auto text-slate-300"/><p className="mt-2 text-xs font-semibold">No bookmarks yet</p><p className="mt-1 text-[10px] text-slate-400">Bookmark useful AI answers to find them here.</p></div>}</div></Modal>}
