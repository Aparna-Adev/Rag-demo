import { BarChart3, BookOpen, FileUp, MessageSquareText, TrendingUp } from 'lucide-react'
import { motion } from 'framer-motion'
import { useApp } from '../context/AppContext'
import { Card } from './ui/Card'

export default function DashboardAnalytics(){const {documents,messages,confidence,recentQuestions}=useApp();const cards=[['Total Policies',documents.length,BookOpen,'text-blue-600 bg-blue-50'],['Questions Asked',messages.filter(m=>m.role==='user').length,MessageSquareText,'text-purple-600 bg-purple-50'],['Documents Uploaded',documents.filter(d=>d.uploaded).length,FileUp,'text-amber-600 bg-amber-50'],['Average Confidence',`${confidence||92}%`,TrendingUp,'text-emerald-600 bg-emerald-50']] as const;const bars=[62,78,55,91,74,84,68];return <motion.section initial={{opacity:0,y:8}} animate={{opacity:1,y:0}} className="min-w-0 flex-1 overflow-y-auto bg-slate-50 p-5 sm:p-7"><div className="mx-auto max-w-6xl"><div><p className="text-xs font-semibold text-blue-600">WORKSPACE OVERVIEW</p><h2 className="mt-1 text-2xl font-bold">HR Assistant Dashboard</h2><p className="mt-1 text-sm text-slate-500">Monitor policy coverage and employee engagement.</p></div><div className="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(([label,value,Icon,tone],i)=><motion.div key={label} initial={{opacity:0,y:12}} animate={{opacity:1,y:0}} transition={{delay:i*.07}}><Card className="p-5"><div className={`grid h-10 w-10 place-items-center rounded-xl ${tone}`}><Icon size={19}/></div><p className="mt-4 text-2xl font-bold">{value}</p><p className="mt-1 text-xs text-slate-500">{label}</p></Card></motion.div>)}</div><div className="mt-5 grid gap-5 lg:grid-cols-[1.5fr_1fr]"><Card className="p-5"><div className="flex items-center justify-between"><div><h3 className="font-bold">Questions this week</h3><p className="text-xs text-slate-500">Employee policy queries by day</p></div><BarChart3 className="text-blue-600" size={20}/></div><div className="mt-8 flex h-52 items-end justify-between gap-3 border-b border-slate-200 px-2">{bars.map((height,i)=><div key={i} className="flex h-full flex-1 flex-col justify-end"><motion.div initial={{height:0}} animate={{height:`${height}%`}} transition={{duration:.7,delay:i*.06}} className="rounded-t-lg bg-gradient-to-t from-blue-600 to-blue-400"/><span className="mt-2 text-center text-[10px] text-slate-400">{['Mon','Tue','Wed','Thu','Fri','Sat','Sun'][i]}</span></div>)}</div></Card><Card className="p-5"><h3 className="font-bold">Recent Activity</h3><p className="text-xs text-slate-500">Latest workspace events</p><div className="mt-5 space-y-4">{recentQuestions.slice(0,5).map((q,i)=><div key={q} className="flex gap-3"><span className="mt-1.5 h-2 w-2 shrink-0 rounded-full bg-blue-500 ring-4 ring-blue-50"/><div><p className="line-clamp-1 text-xs font-semibold">{q}</p><p className="text-[10px] text-slate-400">Asked {i?`${i+1} hours ago`:'just now'}</p></div></div>)}{!recentQuestions.length&&<p className="text-xs text-slate-400">No recent activity</p>}</div></Card></div></div></motion.section>}
