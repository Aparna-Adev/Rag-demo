import { ArrowRight, FileText } from 'lucide-react'
import { motion } from 'framer-motion'
import { Card } from './ui/Card'

const docs=[{name:'Leave Policy.pdf',meta:'Page 14 · Section 3.2',score:96},{name:'Employee Handbook.pdf',meta:'Page 22',score:89},{name:'Attendance Policy.pdf',meta:'Page 8',score:78}]
export default function RetrievedDocumentsCard({ onViewAll }: { onViewAll:()=>void }) { return <Card className="p-4"><div><h3 className="text-sm font-bold">Retrieved Documents</h3><p className="mt-0.5 text-[10px] text-slate-500">Top sources for this response</p></div><div className="mt-4 space-y-4">{docs.map((d,i)=><div key={d.name}><div className="flex gap-2.5"><span className="grid h-8 w-8 shrink-0 place-items-center rounded-lg bg-blue-50 text-blue-600"><FileText size={15}/></span><div className="min-w-0 flex-1"><div className="flex items-center justify-between gap-2"><p className="truncate text-[11px] font-semibold">{d.name}</p><span className={`text-[11px] font-bold ${i===0?'text-emerald-600':'text-slate-600'}`}>{d.score}%</span></div><p className="text-[9px] text-slate-400">{d.meta}</p></div></div><div className="ml-10 mt-2 h-1 rounded-full bg-slate-100"><motion.div initial={{width:0}} animate={{width:`${d.score}%`}} transition={{duration:.8,delay:i*.1}} className={`h-full rounded-full ${i===0?'bg-emerald-500':'bg-blue-500'}`}/></div></div>)}</div><button onClick={onViewAll} className="mt-4 flex w-full items-center justify-center gap-1.5 border-t border-slate-100 pt-3 text-[11px] font-semibold text-blue-600 hover:text-blue-700">View all sources (5)<ArrowRight size={12}/></button></Card> }
